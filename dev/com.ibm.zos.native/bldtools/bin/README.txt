
NOTE: The following scripts must use a LF for the end of line character
      and must be marked executable (right-click -> Properties):

    do-remote-build.sh
    do-ezwas-install.sh
    do-ezwas-test-run.sh
    sync-tree.sh
    
NOTE: The following scripts must be CRLF     
    ezwas-ftp.sh

If platform is used, the scripts won't run under the cygwin environment.

All other files should use 'PLATFORM' as the end of line character.