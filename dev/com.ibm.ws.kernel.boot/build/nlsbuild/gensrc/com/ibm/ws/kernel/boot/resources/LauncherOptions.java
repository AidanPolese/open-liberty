/**
 * WARNING!  THIS FILE IS AUTOMATICALLY GENERATED!  DO NOT MODIFY IT!
 * Generated on Wed Mar 08 13:02:28 EST 2017
 */

package com.ibm.ws.kernel.boot.resources;

public class LauncherOptions extends java.util.ListResourceBundle
{
   public Object[][] getContents() {
       return resources;
   }
   private final static Object[][] resources= {
      { "action-desc.create", "\tCreate a new server if the specified server does not exist. The      \n\t--template option can be used to specify a template to use when      \n\tcreating a new server." },
      { "action-desc.debug", "\tRun the named server in the console foreground after a debugger      \n\tconnects to the debug port (default: 7777)." },
      { "action-desc.dump", "\tDump diagnostic information from the server into an archive. The     \n\t--archive option can be used.  The --include option can be used with \n\tthe \"heap\", \"system\" and \"thread\" values." },
      { "action-desc.help", "\tPrint help information." },
      { "action-desc.javadump", "\tDump diagnostic information from the server JVM. The --include       \n\toption can be used with the \"heap\" and \"system\" values." },
      { "action-desc.list", "\tList the Liberty profile application servers that are defined." },
      { "action-desc.package", "\tPackage a server to an archive. The --archive option can be used.    \n\tThe --include option can be used with values \"all\", \"usr\", \"minify\", \n\t\"wlp\", \"runnable\", \"all,runnable\", and \"minify,runnable\". The values \n\t\"runnable\" and \"all,runnable\" are equivalent. The \"runnable\" value   \n\tworks with \"jar\" type archives only." },
      { "action-desc.pause", "\tPause all the components in the server that can be paused. You can   \n\tpause a subset of the components by specifying the components on the \n\t--target option." },
      { "action-desc.registerWinService", "\tRegister the server specified as a Windows Service program." },
      { "action-desc.resume", "\tResume all paused components in the server. You can specify the      \n\t--target option to resume specific paused components." },
      { "action-desc.run", "\tRun the named server in the console foreground." },
      { "action-desc.start", "\tStart the named server." },
      { "action-desc.startWinService", "\tStart the server specified as a Windows Service program.             \n\tIt must be registered before starting." },
      { "action-desc.status", "\tCheck the status of the named server." },
      { "action-desc.stop", "\tStop the running instance of the named server." },
      { "action-desc.stopWinService", "\tStop the server specified as a Windows Service program." },
      { "action-desc.unregisterWinService", "\tUnregister the server specified as a Windows Service program." },
      { "action-desc.version", "\tShow server version information and exit." },
      { "action-key.create", "    --create" },
      { "action-key.debug", "    --debug" },
      { "action-key.dump", "    --dump" },
      { "action-key.help", "    --help" },
      { "action-key.javadump", "    --javadump" },
      { "action-key.list", "    --list" },
      { "action-key.package", "    --package" },
      { "action-key.pause", "    --pause" },
      { "action-key.registerWinService", "    --registerWinService" },
      { "action-key.resume", "    --resume" },
      { "action-key.run", "    --run" },
      { "action-key.start", "    --start" },
      { "action-key.startWinService", "    --startWinService" },
      { "action-key.status", "    --status" },
      { "action-key.stop", "    --stop" },
      { "action-key.stopWinService", "    --stopWinService" },
      { "action-key.unregisterWinService", "    --unregisterWinService" },
      { "action-key.version", "    --version" },
      { "briefUsage", "Usage: java [JVM options] -javaagent:bin/tools/ws-javaagent.jar \\        \n\t-jar bin/tools/ws-server.jar serverName [actions] [options]  " },
      { "javaAgent.desc", "\tThis is a JVM option used to specify an agent for instrumentation.   \n\tThe runtime uses instrumentation for gathering trace and other debug \n\tinformation. The bootstrap-agent.jar is be in the same directory as  \n\tthe jar used to launch the runtime." },
      { "javaAgent.key", "    -javaagent:bin/tools/ws-javaagent.jar" },
      { "option-desc.archive", "\tSpecify the archive target to be generated by the package or dump    \n\taction. The target can be specified either as an absolute path or    \n\tas a relative path. If this option is omitted, the archive file will\n\tbe created in the server output directory. The target file name      \n\textension might influence the format of the generated archive.       \n\tThe default archive format for the package action is \"pax\" on z/OS   \n\tand \"zip\" on all other platforms.                                    \n\tArchive format \"jar\" will produce a self-extracting jar similar to   \n\tthe original installer archive.                                      \n\tArchive format \"jar\" combined with \"runnable\" on the --include option\n\tproduces a runnable jar file that can run the Liberty server from    \n\tthe jar file by using java -jar." },
      { "option-desc.clean", "\tClean all cached information related to this server instance." },
      { "option-desc.include", "\tA comma-delimited list of values. The valid values vary depending on \n\tthe action." },
      { "option-desc.os", "\tSpecifies the operating systems that you want the packaged server to \n\tsupport. Supply a comma-separated list. The default value is any,    \n\tindicating that the server is to be deployable to any operating      \n\tsystem supported by the source.                                      \n\tTo specify that an operating system is not to be supported, prefix it\n\twith a minus sign (\"-\"). For a list of operating system values, refer\n\tto the OSGi Alliance web site at the following URL:                  \n\thttp://www.osgi.org/Specifications/Reference#os                      \n\tThis option applies only to the package operation, and can be used   \n\tonly with the --include=minify option. If you exclude an operating   \n\tsystem, you cannot later include it if you repeat the minify         \n\toperation on the archive. " },
      { "option-desc.target", "\tSpecifies a comma-delimited list of components that you can pause or \n\tresume." },
      { "option-desc.template", "\tSpecify the name of the template to use when creating a new server. " },
      { "option-key.archive", "    --archive=\"path to the target archive file\"" },
      { "option-key.clean", "    --clean" },
      { "option-key.include", "    --include=value,value,..." },
      { "option-key.os", "    --os=value,value,..." },
      { "option-key.target", "    --target=value,value" },
      { "option-key.template", "    --template=\"templateName\"" },
      { "processName.desc", "\tA locally unique name for the server; the name can be constructed    \n\tusing Unicode alphanumeric characters (for example, A-Za-z0-9), the  \n\tunderscore (_), dash (-), plus (+) and period (.). A server name     \n\tcannot begin with a dash (-) or period (.)." },
      { "processName.key", "    serverName" },
      { "scriptUsage", "Usage: {0} action serverName [options]" },
      { "use.actions", "Actions:" },
      { "use.jvmarg", "JVM options:" },
      { "use.options", "Options:" }
   };
}
